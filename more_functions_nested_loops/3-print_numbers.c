#include "main.h"
/**
 *print_numbers - check is digit
 *
 *
 *Return: 1 if c is uppercase, 0 otherwise
 */

void print_numbers(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
